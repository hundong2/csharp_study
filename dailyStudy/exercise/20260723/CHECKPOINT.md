# 초보자 확인 단계

코드를 보지 않고 답한 뒤 README와 `Program.cs` 주석으로 확인하세요.

1. `string?`의 `?`는 무엇을 허용하나요?
2. `record`와 `with`가 공유 데이터 변경 위험을 줄이는 이유는 무엇인가요?
3. `Any`와 `Where`의 결과 차이는 무엇인가요?
4. `await` 중 스레드를 붙잡지 않는 것이 서버에서 왜 유리한가요?
5. 중복 예약은 Result, null 인수는 예외로 처리한 기준은 무엇인가요?
6. Domain Model, Application Service, Repository, Strategy의 책임을 한 문장씩 말해 보세요.
7. Composition Root와 생성자 주입이 테스트를 쉽게 만드는 이유는 무엇인가요?
8. 실제 운영에서 메모리 Repository를 DB로 바꿀 때 동시성, 로그, 메트릭, 재시도 중 무엇을 고려해야 하나요?

완료 기준: 일반 실행과 `--self-test`가 모두 성공하고, 위 질문 중 6개 이상을 자신의 말로 설명할 수 있습니다.
