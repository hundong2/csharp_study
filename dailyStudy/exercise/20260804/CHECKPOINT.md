# 초보자 확인 단계

다음 질문에 코드 없이 말로 답할 수 있으면 오늘 목표를 달성한 것입니다.

1. `string`과 `string?`의 차이는 무엇인가요?
2. 돈 계산에 `decimal`을 사용한 이유는 무엇인가요?
3. `record`로 명령과 결과를 표현하면 어떤 실수를 줄일 수 있나요?
4. 예상 가능한 실패는 Result, 예상 밖 기술 장애는 예외로 구분한 이유는 무엇인가요?
5. Strategy와 Repository는 각각 무엇을 바꾸기 쉽게 하나요?
6. DI와 Composition Root가 self-test를 쉽게 만든 지점을 찾을 수 있나요?
7. `await`와 `CancellationToken`은 운영 서비스에서 왜 필요한가요?

마지막으로 `dotnet run -- --self-test`를 실행해 `4/4 통과`를 확인하세요.
