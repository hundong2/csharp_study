# Modern C# 및 .NET 데일리 리포트 - 2026-07-05

오늘은 **실전 대규모 트래픽 장애 방어 편 2탄: 메모리 누수 탐지, POH 자율 스로틀링, ClrMD 스타일 라이브 힙 스캔**입니다.

> 첨부에서 7월 5일 원문은 빠져 있었기 때문에, 7월 6일 리포트의 전날 요약을 바탕으로 실습 가능한 브리지 편으로 보강했습니다.

## 실행 명령

```powershell
cd dailyStudy/20260705
dotnet script 1_PohThrottleBuffer.csx
dotnet script 2_WeakReferenceLeakProbe.csx
dotnet script 3_GcPressureCircuitBreaker.csx
dotnet script 4_ClrMdRootPathSimulator.csx
dotnet script 5_ObjectPoolRecovery.csx
```

## 1. POH 스타일 고정 버퍼 스로틀링

고정된 버퍼가 무한정 늘어나면 GC와 힙 단편화에 부담이 됩니다. 실습에서는 `GC.AllocateArray(..., pinned: true)`로 고정 배열을 만들고, 최대 개수를 넘으면 할당을 거절하는 스로틀을 구현합니다.

- [1_PohThrottleBuffer.csx](./1_PohThrottleBuffer.csx)

## 2. WeakReference 기반 누수 감지

객체가 더 이상 강하게 참조되지 않는지 확인할 때 `WeakReference<T>`를 사용합니다. 실습은 캐시에서 제거된 객체가 GC 이후 회수 가능한 상태인지 관찰합니다.

- [2_WeakReferenceLeakProbe.csx](./2_WeakReferenceLeakProbe.csx)

## 3. GC 압박 회로 차단기

메모리 사용량이 임계치를 넘으면 새 작업을 받아들이지 않는 간단한 회로 차단기를 만듭니다.

- [3_GcPressureCircuitBreaker.csx](./3_GcPressureCircuitBreaker.csx)

## 4. ClrMD 스타일 Root Path 추적 시뮬레이터

실제 ClrMD는 프로세스의 힙과 루트 참조를 분석합니다. 여기서는 객체 그래프를 딕셔너리로 모델링해 루트에서 누수 후보까지의 경로를 찾습니다.

- [4_ClrMdRootPathSimulator.csx](./4_ClrMdRootPathSimulator.csx)

## 5. ObjectPool 기반 장애 후 회복

대량 요청에서 임시 버퍼를 매번 새로 만들면 GC 압박이 커집니다. 실습에서는 간단한 풀을 만들어 재사용하는 패턴을 확인합니다.

- [5_ObjectPoolRecovery.csx](./5_ObjectPoolRecovery.csx)

