# Modern C# 및 .NET 데일리 리포트 - 2026-07-04

오늘부터 **실전 대규모 트래픽 장애 방어 편: 고성능 데드락 탐지, 스레드 풀 정체 우회 및 메모리 진단 기법** 시리즈를 시작합니다.

> 실습 메모: 원문에는 C# 14 `extension type`, 자동 구현 속성의 `field` 같은 최신/가상 문법이 포함되어 있습니다. 현재 `dotnet script`에서 안정적으로 실행되도록 실습 코드는 어댑터 클래스, 확장 메서드, 명시적 백킹 필드로 재구성했습니다.

## 실행 명령

```powershell
cd dailyStudy/20260704
dotnet script 1_AsyncStallDetector.csx
dotnet script 2_ExtensionProfiler.csx
dotnet script 3_FieldTicketQueue.csx
dotnet script 4_CacheContentionTrace.csx
dotnet script 5_AsyncHierarchyInline.csx
```

## 1. Lock-Free Async-Stall Detection

외부 API 호출이 지연될 때 워커 스레드를 오래 붙잡지 않도록 타임아웃과 계측 포인트를 명확히 둡니다. 실습은 네트워크 의존성을 없애기 위해 `HttpMessageHandler`를 사용한 가짜 응답으로 구성했습니다.

- [1_AsyncStallDetector.csx](./1_AsyncStallDetector.csx)

## 2. Extension Type 개념 기반 프로파일러 인터셉터

수정할 수 없는 기존 클래스 앞뒤에 지연 시간 측정을 덧붙입니다. 실제 C# 14 확장 타입 대신, 실행 가능한 어댑터로 같은 구조적 의도를 연습합니다.

- [2_ExtensionProfiler.csx](./2_ExtensionProfiler.csx)

## 3. field 키워드 개념 기반 티켓 디스패처

스레드 풀 정체를 직접 해결하는 마법은 없지만, 진입량을 원자적으로 계수하는 티켓 게이트는 장애 방어의 기본 부품입니다.

- [3_FieldTicketQueue.csx](./3_FieldTicketQueue.csx)

## 4. EventPipe Lock-Contention Tracing 개념

캐시 스탬피드나 대량 무효화 상황에서 락 대기 시간을 낮은 비용으로 수집하는 모델을 단순화해 확인합니다.

- [4_CacheContentionTrace.csx](./4_CacheContentionTrace.csx)

## 5. Async Hierarchy Inlining 개념

`ValueTask`가 즉시 완료되는 핫패스에서 async 계층의 비용을 줄이는 설계를 연습합니다.

- [5_AsyncHierarchyInline.csx](./5_AsyncHierarchyInline.csx)

