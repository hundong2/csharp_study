# 13. DateTimeOffset, Regex, TryParse

날짜, 문자열 파싱, 정규식은 실무에서 자주 나오는 기본 도구입니다.

## 핵심 개념

- `DateTimeOffset`: 시간대 오프셋을 포함하는 날짜/시간입니다.
- `TryParse`: 실패 가능성이 있는 변환을 예외 없이 처리합니다.
- `Regex`: 문자열 패턴을 검사하거나 추출합니다.

## 실무에서 왜 필요한가

외부 입력은 항상 깨질 수 있습니다. `Parse`보다 `TryParse`를 우선 검토하면 장애를 줄일 수 있습니다.
