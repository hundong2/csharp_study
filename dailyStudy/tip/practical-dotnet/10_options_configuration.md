# 10. Options와 Configuration

설정값은 문자열로 들어오지만, 애플리케이션 내부에서는 강한 타입 객체로 다루는 편이 안전합니다.

## 핵심 개념

- Options 클래스는 설정 묶음입니다.
- `required`와 `init`으로 초기화 규칙을 명확히 합니다.
- 앱 시작 시 검증해야 운영 중 장애를 줄일 수 있습니다.

## 실무에서 왜 필요한가

잘못된 URL, 포트, timeout 값은 런타임 장애로 이어집니다. 설정 검증을 앱 시작 시점에 끝내야 장애 범위를 줄일 수 있습니다.
